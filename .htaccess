RewriteEngine On

# Keep direct navigation working for the React app.
RewriteRule ^api(?:\.php)?(?:/.*)?$ - [END]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
